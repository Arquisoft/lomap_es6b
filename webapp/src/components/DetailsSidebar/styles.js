import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
    gridLogo: {
        display: 'flex',
        justifyContent: 'center',
    },

    gridSearchPlacesBar: {
        display: 'flex',
        justifyContent: 'center',

    },

    gridFilterByBar: {
        display: 'flex',
        justifyContent: 'center',
    }

}));
