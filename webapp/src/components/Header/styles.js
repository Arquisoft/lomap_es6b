import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
    gridLogo: {
        display: 'flex',
        justifyContent: 'center',
    },

    gridSearchPlacesBar: {
        display: 'flex',
        justifyContent: 'center',

    },

    gridFilterByBar: {
        display: 'flex',
        justifyContent: 'center',
    },

    logoIcon: {
        width: 'auto',
        height: '60px',
        paddingBottom: '10px',
    }

}));